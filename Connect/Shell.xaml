<Window
    x:Class = "SAMS.Connect.Shell"
    xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels = "clr-namespace:SAMS.Connect.MVVM.ViewModels"
    xmlns:views = "clr-namespace:SAMS.Connect.MVVM.Views"
    d:DataContext = "{d:DesignInstance viewmodels:ShellViewModel}"
    Icon = "../Assets/Icon/appicon.ico"
    mc:Ignorable = "d"
    Title = "SAMS Connect"
    WindowStartupLocation = "CenterScreen"
    MinHeight = "600"
    MinWidth = "800"
    Height = "800"
    Width = "1252">

    <!-- Nav-Graph -->
    <!--
        This serves as the primary navigation graph for the application.
        Using the singleton instance of the NavigationService, views can request
        navigation events via relay commands through their ViewModels. When the
        type of the bound object changes to a mapped ViewModel the corresponding
        view is loaded in-place within the shell window.
    -->
    <!--
        ATTRIBUTION:
        SPA navigation using the service and DataTemplates were heavily adapted from GitHub.
        - https://github.com/vain0x/wpf-navigation-example
        - vain0x (https://github.com/vain0x)
    -->
    <Window.Resources>
        <DataTemplate
            DataType = "{x:Type viewmodels:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
        <DataTemplate
            DataType = "{x:Type viewmodels:TicketUpsertViewModel}">
            <views:TicketUpsertView />
        </DataTemplate>
    </Window.Resources>

    <ContentControl
        Content = "{Binding NavGraph.CurrentViewModel}" />

</Window>
